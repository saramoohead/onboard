<%= simple_form_for @room do |form| %>

	<%= form.input :name %>
	<%= form.input :address %>
	<%= form.input :description %>
	<%= form.input :image %>
	<%= form.input :number_of_guests %>
	<%= form.input :price_in_pence %>

	<%= form.button :submit %>

<% end %>

